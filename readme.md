# 📖 Документация: Автоматический обмен сообщениями между чатами

## 🎯 Назначение программы

Программа автоматизирует обмен сообщениями между двумя веб-чатами:
- **JuicyChat.AI** (вкладка 0)
- **DeepSeek** (вкладка 1)

Скрипт выполняет циклический обмен сообщениями: копирует последнее сообщение из одного чата и отправляет его в другой.

## 📋 Требования

### 1. Программное обеспечение
- **Python 3.7+**
- **Google Chrome** (или другой браузер)
- **Selenium WebDriver** (`pip install selenium`)

### 2. Установка зависимостей
```bash
pip install selenium
```

## 🚀 Подготовка к запуску

### Шаг 1: Запуск Chrome в режиме отладки
Перед запуском скрипта необходимо запустить Chrome с параметрами отладки:

**Windows:**
```bash
"C:\Program Files\Google\Chrome\Application\chrome.exe" --remote-debugging-port=9222 --user-data-dir="C:\selenium_chrome_profile"
```

**macOS/Linux:**
```bash
/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome --remote-debugging-port=9222 --user-data-dir="/tmp/chrome_profile"
```

### Шаг 2: Открытие вкладок
В запущенном браузере откройте две вкладки:
1. **Вкладка 0:** `https://juicychat.ai` (или ваш конкретный чат JuicyChat)
2. **Вкладка 1:** `https://chat.deepseek.com` (или другая страница DeepSeek)

### Шаг 3: Авторизация
**Вручную выполните вход** в оба чата. Программа работает с уже авторизованными сессиями.

## ⚙️ Настройки программы

Основные настройки находятся в начале скрипта:

```python
# Основные настройки
WAIT_TIME = 10          # Время ожидания между действиями (секунды)
MAX_CYCLES = 30         # Количество циклов обмена
JC_INPUT_SELECTOR = "textarea[class*='_sendInput_']"    # Селектор поля ввода JC
JC_MESSAGE_SELECTOR = "div._markdownContent_8vtsi_181"  # Селектор сообщений JC
```

### Настройка под ваши чаты
Если селекторы не работают, обновите их:
1. Откройте DevTools (F12)
2. Найдите поле ввода/сообщения
3. Скопируйте актуальный CSS-селектор

## 📝 Запуск программы

### 1. Сохранение скрипта
Сохраните код в файл `chat_exchange.py`

### 2. Запуск
```bash
python chat_exchange.py
```

### 3. Остановка
- **Ctrl+C** в терминале для остановки
- Программа завершит текущий цикл и остановится

## 🔄 Работа программы

### Структура одного цикла (≈60 секунд)

```
ЦИКЛ X/30:
  ФАЗА 1: JC → DeepSeek (≈30 секунд)
    ├── Переключение на JuicyChat
    ├── Копирование последнего сообщения
    ├── Переключение на DeepSeek
    ├── Вставка текста + добавление точки
    ├── Удаление точки + отправка
    └── Ожидание 10 секунд ответа

  ФАЗА 2: DeepSeek → JC (≈30 секунд)
    ├── Копирование ответа DeepSeek
    ├── Переключение на JuicyChat
    ├── Вставка текста + отправка
    └── Ожидание 10 секунд перед следующим циклом
```

### Особенности отправки

#### Для DeepSeek:
- **Вставка текста** через JavaScript
- **Добавление точки** (`.`) в конец
- **Удаление точки** (Backspace)
- **Отправка** (Enter)

#### Для JuicyChat:
- **Печать текста** через `send_keys()`
- **Активация поля** (пробел + Backspace)
- **Отправка** (Enter)

## ⚠️ Возможные проблемы и решения

### Проблема 1: Chrome не запускается в режиме отладки
**Решение:** Убедитесь, что все окна Chrome закрыты перед запуском команды.

### Проблема 2: Селекторы не находят элементы
**Решение:** Обновите селекторы в настройках программы.

### Проблема 3: Сообщения отправляются частями
**Решение:** Увеличьте `WAIT_TIME` или проверьте метод отправки.

### Проблема 4: Скрипт не переключает вкладки
**Решение:** Убедитесь, что открыто ровно 2 вкладки в указанном порядке.

## 📊 Мониторинг работы

Программа выводит подробные логи:
- **Номер текущего цикла**
- **Количество символов** в сообщениях
- **Статус каждой операции**
- **Обратный отсчет** времени ожидания

Пример вывода:
```
[PHASE 1] JC -> DeepSeek
[1.1] Switching to JuicyChat...
[OK] Switched to tab 0 (JuicyChat)
[JC] Copied message (245 chars)
[WAIT 10s] JC message ready, switching to DS
10...
5...
3...
2...
1...
```

## 🔧 Расширенные настройки

### Изменение времени ожидания
```python
WAIT_TIME = 15  # Увеличить до 15 секунд
```

### Бесконечный цикл
```python
MAX_CYCLES = 0  # 0 = бесконечный цикл
```

### Быстрая печать
```python
# В функции type_text_with_events():
batch_size = 100    # Увеличить размер батча
delay = 0.01        # Уменьшить задержку
```

## 🛑 Безопасность и ограничения

### Важные предупреждения:
1. **Не нарушайте правила сервисов** - проверьте ToS чатов
2. **Сохраняйте конфиденциальность** - не используйте с приватными данными
3. **Тестируйте на тестовых аккаунтах** - перед использованием на основных

### Ограничения программы:
- Работает только с уже открытыми вкладками
- Требует стабильного интернет-соединения
- Зависит от структуры HTML чатов (может сломаться при обновлении интерфейса)

## 📞 Поддержка и обновления

При возникновении проблем:
1. **Сохраните вывод консоли**
2. **Проверьте актуальность селекторов**
3. **Убедитесь в правильной настройке Chrome**

Для обновления селекторов используйте DevTools:
- **Поле ввода:** `Copy → Copy selector`
- **Сообщения:** Найдите уникальный класс элемента

---

**Готово к запуску!** Программа автоматизирует общение между чатами, экономя время и обеспечивая непрерывный диалог.